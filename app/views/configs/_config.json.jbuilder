json.extract! config, :id, :name, :active_config, :title, :hotfolder, :max_time, :day, :load_teams, :csv_delimiter, :unique_id, :firstname, :lastname, :entry_class, :gender, :classifier, :time, :school, :team, :jrotc, :other_class, :other_class_title, :created_at, :updated_at
json.url config_url(config, format: :json)
